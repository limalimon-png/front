import { ImagenSanitazerPipe } from './imagen-sanitazer.pipe';

describe('ImagenSanitazerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenSanitazerPipe();
    expect(pipe).toBeTruthy();
  });
});
