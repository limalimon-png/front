import { ImagenPerfilPipe } from './imagen-perfil.pipe';

describe('ImagenPerfilPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenPerfilPipe();
    expect(pipe).toBeTruthy();
  });
});
